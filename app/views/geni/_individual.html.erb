<div id="show-individual"
     style="font-size: <%=font%>px; min-width: <%= font*12%>px" >	
		    <%= sex_symbol( individual.sex ) %> 
		    <% if @individual == individual and is_editor? %>
		      <span class="individual-edit">
                <%= link_to( individual.pretty_name(is_user?), 
		               edit_path( individual.uid ), remote: true ) if individual %>
		      </span>		               	               
		    <% else %>
		      <%= link_to( individual.pretty_name(is_user?),
		                  tree_path( individual.uid ) ) if individual %>	
		    <% end %>	  
		    <% if @individual == individual and is_admin? %>
              <%= link_to( '<small><i>details</i></small>'.html_safe, 
                       individual_by_uid_path( individual.uid ) ) if individual %>	
		    <% end %>	
            <% if is_editor? and @individual != individual and 
                ( @individual.is_my_child?( individual ) ) %>
              <%= link_to delete_symbol, 
		         remove_child_path( individual.uid, @individual.uid ), class: 'xdelete',
		         data: { confirm: 'Are you sure?' } %>
		    <% end %>			    	      	  
	        <%= "<br>*".html_safe if individual and individual.birth %>
	           <%= individual.birth.date if individual.birth %><% individual.birth.location if individual.birth %><%= "<br>&#10013;".html_safe if individual.death %>
	           <%= individual.death.date if individual.death %>
	           <% individual.death.location if individual.death %>	           
</div>	           
         