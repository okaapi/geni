<h1>First names for <%= @surname %>... </h1>

<% @names.each_with_index do |name, index| %>
  
  <%= link_to name[:given], 
      tree_path( name[:uid] ) %>
  <%= '|' if index < @names.count-1 %> 
  
<% end %>

<br>
<br>
<br>
<strong> <%= link_to 'Home', root_path %> </strong> <br>

<%= render partial: "footer" %>

